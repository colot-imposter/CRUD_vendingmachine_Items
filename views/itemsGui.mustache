<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../public/style/style.css">
    <title>Items to Vend</title>
  </head>
  <body>
    <h1>Items to Vend</h1>

    <div class="addItemButton">
      Add a <a href="/newItem">New Item</a>
    </div>

      <table>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Quantity</th>
        <th>Price</th>
      </tr>
      {{#itemsList}}
      <tr>
      <!-- <td><img src="../public/photos/giphy.gif" alt="german-dancing like carlton from fresh house, but better"></td> -->
      <td><a href='/update_item/{{dataValues.id}}'>{{dataValues.name}}</a></td>
      <td>{{dataValues.description}}</td>
      <td>{{dataValues.quantity}}</td>
      <td>{{dataValues.price}}</td>
      <td> <form action="/delete_item/{{id}}" method="post">
        <button type="submit">Delete</button>
      </form></td>
      </tr>
      {{/itemsList}}
      </table>
  </body>
</html>
